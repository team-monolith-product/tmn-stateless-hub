# tmn-stateless-hub fork 의 dev 환경 전용 추가 의존성.
#
# requirements-tmn.txt (공통) 위에 dev 이미지에만 install 된다. Dockerfile
# 의 tmn-stateless-hub-dev target 이 본 파일을 install 한다. 디버깅·프로파일링
# 도구처럼 prd 에 포함하면 attack surface 가 늘어나는 의존성을 분리할 때 사용.
#
# 현재는 빈 placeholder. 필요한 패키지가 생기면 한 줄씩 추가한다.
